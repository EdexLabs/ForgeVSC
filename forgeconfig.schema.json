{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ForgeConfig",
  "type": "object",
  "properties": {
    "extensions": {
      "type": "array",
      "description": "GitHub extension shorthands (e.g. 'github:user/repo#branch') or detailed metadata energy objects.",
      "items": {
        "oneOf": [
          {
            "type": "string",
            "pattern": "^github:[^/]+/[^#]+#.+",
            "description": "GitHub shorthand: github:username/repo#branch"
          },
          {
            "type": "object",
            "required": ["extension"],
            "properties": {
              "extension": {
                "type": "string",
                "description": "Name of the extension (e.g. 'forge-std')"
              },
              "functions": {
                "type": "string",
                "format": "uri",
                "description": "URL to functions.json"
              },
              "enums": {
                "type": "string",
                "format": "uri",
                "description": "URL to enums.json"
              },
              "events": {
                "type": "string",
                "format": "uri",
                "description": "URL to events.json"
              }
            }
          }
        ]
      }
    },
    "custom_functions_path": {
      "oneOf": [
        {
          "type": "string",
          "description": "Path to a folder (relative to forgeconfig.json) to scan for custom function definitions."
        },
        {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of paths to folders (relative to forgeconfig.json) to scan for custom function definitions."
        }
      ]
    },
    "custom_colors": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^#([A-Fa-f0-9]{6}|[A-Fa-f0-9]{3})$"
      },
      "description": "Custom hex colors for functions."
    }
  }
}